{
  "name": "ai-voice-dialer-backend",
  "version": "1.0.0",
  "private": true,
  "description": "AI outbound voice dialer — Retell custom-LLM turn loop + queue scheduler. Vertical-agnostic starter.",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "scheduler": "node scheduler.js",
    "test": "node --test"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "ws": "^8.18.0"
  }
}
